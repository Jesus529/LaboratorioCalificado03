<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center"
    android:background="@drawable/background_gradient"
    tools:ignore="ExtraText"> <!-- Fondo con gradiente para darle un toque más moderno -->

    <!-- Encabezado con el texto de llamada -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Llamando al Profesor..."
        android:textSize="26sp" <!-- Texto más grande para mayor impacto -->
    android:textStyle="bold"
    android:textColor="#FFFFFF" <!-- Blanco para mejor contraste -->
    android:layout_marginBottom="24dp"
    android:shadowColor="#000000"
    android:shadowDx="2"
    android:shadowDy="2"
    android:shadowRadius="4" /> <!-- Agregar sombra al texto para hacerlo más legible y estilizado -->

    <!-- Imagen de llamada -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="24dp"
        android:src="@drawable/contacto" <!-- Asegúrate de tener un ícono de llamada apropiado -->
    android:contentDescription="Imagen de llamada"
    android:scaleType="centerCrop"
    android:background="@drawable/circular_background" /> <!-- Fondo circular para la imagen, con borde -->

    <!-- Información del profesor -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Profesor: Victor Alejandro Saico Justo"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="8dp"
        android:shadowColor="#000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="2" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Teléfono: +51 925137361"
        android:textSize="16sp"
        android:textColor="#DDDDDD" <!-- Texto ligeramente más claro para dar un contraste suave -->
    android:layout_marginBottom="8dp"
    android:shadowColor="#000000"
    android:shadowDx="1"
    android:shadowDy="1"
    android:shadowRadius="2" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Email: vsaico@tecsup.edu.pe"
        android:textSize="16sp"
        android:textColor="#DDDDDD"
        android:layout_marginBottom="24dp"
        android:shadowColor="#000000"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="2" />

    <!-- Botón para finalizar la llamada -->
    <Button
        android:id="@+id/endCallButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Terminar llamada"
        android:textColor="#FFFFFF"
        android:background="@drawable/button_background" <!-- Fondo estilizado para el botón -->
    android:padding="14dp"
    android:layout_gravity="center"
    android:drawableLeft="@drawable/ic_end_call" <!-- Cambia el ícono si es necesario -->
    android:drawablePadding="8dp"
    android:cornerRadius="20dp" <!-- Esquinas más redondeadas para un estilo más suave -->
    android:elevation="8dp" <!-- Sombra sutil para darle profundidad -->
    android:layout_marginTop="24dp" /> <!-- Espaciado superior para separarlo de la información del profesor -->

</LinearLayout>
